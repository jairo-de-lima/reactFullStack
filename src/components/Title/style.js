import styled from "styled-components";

export const DefaulTitle = styled.h2`
  color: #fff;
  text-align: center;
  font-size: 2.5rem;
  font-style: normal;
  font-weight: 600;
  margin-top: 1rem;

  @media (max-width: 750px) {
    font-size: 2rem;
  }
`;
